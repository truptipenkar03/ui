import { Meta, Source } from '@storybook/addon-docs/blocks';
import { Title, SectionTitle } from '../../.storybook/components';

import Logo from '../../.storybook/logo-main.png';

<Meta
  title="Getting Started/Introduction"
/>

<div style={{ display: 'flex', justifyContent: 'center', marginBottom: '100px'}}>
  <img src={Logo} height="200px" />
</div>

<Title
  title="Getting Started"
  subtitle="Reusable React component library designed to create a consistent look and feel."
/>
 
<SectionTitle>Installation</SectionTitle>

Using npm or yarn.
```
npm install --save @rtkwlf/ui react react-dom react-is styled-components react-select
```
or
```
yarn add @rtkwlf/ui react react-dom react-is
```

<SectionTitle>Usage</SectionTitle>

Import the components you would like to use in your React Application:

`import { Button } from @rtkwlf/ui;`

Then use them in a component:
```
const example = () => (
  <Button>Click Me!</Button>
);
```





