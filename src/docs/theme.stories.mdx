import { Meta, Source } from '@storybook/addon-docs/blocks';
import { Title, SectionTitle, ThemeDisplay } from '../../.storybook/components';

import Logo from '../../.storybook/logo-main.png';

<Meta
  title="Getting Started/Theming"
/>

<Title
  title="Theming"
  subtitle="Provide a theme to change the look and feel of each component"
/>

<SectionTitle>Creating a Theme</SectionTitle>

If you wish to create your own theme use the provided `createTheme` function. Each component provides its own theme
 that can be overridden. Please see the respective component documentation for a full list of the available theme
  overrides.

```
import { createTheme } from '@rtkwlf/ui';

const myTheme = createTheme(colorOverrides, themeOverrides);
```

<SectionTitle>Using a Theme</SectionTitle>

Once you have created a new theme you can provide it to the components via the `ThemeProvider`.

```
import { createTheme, ThemeProvider } from '@rtkwlf/ui';

const myTheme = createTheme({
  primary: 'red'
}, {});

const App = () => (
  <ThemeProvider theme={myTheme}>
    <Button>My Red Button</Button>
  </ThemeProvider
)
```

<SectionTitle>Color Palette</SectionTitle>

The following is the color palette used for the components.

<ThemeDisplay colors />

<SectionTitle>Theme</SectionTitle>

The following theme is available to be overridden.

<ThemeDisplay />




