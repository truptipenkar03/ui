import { Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { Title, SectionTitle } from '../../../../.storybook/components';
import { Floater } from '../Floater';

<Title
  title="Floater"
  subtitle="Floaters all users to display information with respect to an anchor point"
/>

<SectionTitle>Simple</SectionTitle>

Add a simple floater to any anchor point using a `ref`.

<Preview>
  <Story id="components-floater--simple"/>
</Preview>

<SectionTitle>Complex</SectionTitle>

A floater can "float" around the screen, simply by change the anchor point. The target anchor can be retrieved from the `click` event on the button.

<Preview>
  <Story id="components-floater--complex"/>
</Preview>

<SectionTitle>Props</SectionTitle>
<Props of={Floater} />
