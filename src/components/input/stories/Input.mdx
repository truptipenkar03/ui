import { Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { Title, SectionTitle } from '../../../../.storybook/components';
import { Input } from '../Input';

<Title
  title="Input"
  subtitle="Inputs allow users to input data."
/>

<SectionTitle>Simple</SectionTitle>

Many times a simple input is all that is needed.

<Preview>
  <Story id="components-input--simple"/>
</Preview>

<SectionTitle>Size</SectionTitle>

Change the size of the input by modifying the `inputSize` prop.

<Preview>
  <Story id="components-input--size"/>
</Preview>

<SectionTitle>Types</SectionTitle>

Change the type of the input by modifying the `htmlType` prop.

<Preview>
  <Story id="components-input--types"/>
</Preview>

<SectionTitle>Border</SectionTitle>

Modify how the `input` looks by changing the `borderType` prop;

<Preview>
  <Story id="components-input--border"/>
</Preview>

<SectionTitle>Label</SectionTitle>

Provide a `label` for the input to use in a form.

<Preview>
  <Story id="components-input--label"/>
</Preview>

<SectionTitle>Disabled</SectionTitle>

Disable an input by adding passing it the `disabled` prop. This will inhibit the users ability to focus and type in
 the input.

<Preview>
  <Story id="components-input--disabled"/>
</Preview>

<SectionTitle>Props</SectionTitle>
<Props of={Input} />
