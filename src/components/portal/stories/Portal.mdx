import { Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { Title, SectionTitle } from '../../../../.storybook/components';
import { Portal } from '../Portal';

<Title
  title="Portal"
  subtitle="Portals allow components to mount to a position outside the default DOM position."
/>

<SectionTitle>Simple</SectionTitle>

Add a simple floater to any anchor point using a `ref`.

<Preview>
  <Story id="components-portal--simple"/>
</Preview>

<SectionTitle>Props</SectionTitle>
<Props of={Portal} />
