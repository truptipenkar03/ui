import { Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { Title, SectionTitle } from '../../../../.storybook/components';
import { DropPad } from '../DropPad';

<Title
  title="DropPad"
  subtitle="Use DropPad to get a list of files that can be uploaded"
/>

<SectionTitle>Simple</SectionTitle>

This example provides a simple way to get files that have been dropped or selected from the file picker. It is important to note that these files will not be uploaded. In order to uploaded them, feel free to make use of the handy `useUploadOnMountEffect` hook that we provide.

<Preview>
  <Story id="components-droppad--simple"/>
</Preview>

<SectionTitle>DropPad Props</SectionTitle>
<Props of={ DropPad } />

<SectionTitle>DropPad.File Props</SectionTitle>
<Props of={ DropPad.File } />
