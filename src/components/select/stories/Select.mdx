import { Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { Title, SectionTitle } from '../../../../.storybook/components';
import { Select } from '../Select';

<Title
  title="Select"
  subtitle="Allows users to select from a list of options."
/>

<SectionTitle>Simple</SectionTitle>

Simple single value select.

<Preview>
  <Story id="components-select--simple"/>
</Preview>

<SectionTitle>Multi-Select</SectionTitle>

If there are multiple selections required, set `isMulti` to `true`.

<Preview>
  <Story id="components-select--multi"/>
</Preview>

<SectionTitle>Disabled</SectionTitle>

Disable user iteraction by setting `isDisabled` to `true`.

<Preview>
  <Story id="components-select--disabled" />
</Preview>

<SectionTitle>Select Props</SectionTitle>
<Props of={ Select } />
