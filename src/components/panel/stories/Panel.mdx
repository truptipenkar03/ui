import { Story, Preview, Props, Source, Anchor } from '@storybook/addon-docs/blocks';
import { Title, SectionTitle, ThemeDisplay } from '../../../../.storybook/components';
import { Panel } from '../Panel';
import { theme } from '../../../theme';

<Title
  title="Panel"
  subtitle="Panel provides a simple box to display data in."
/>

<SectionTitle>Standard</SectionTitle>

You can use the standard panel simply by passing it children.

<Preview>
  <Story id="components-panel--standard"/>
</Preview>

<SectionTitle>Clickable</SectionTitle>

You optionally make the panel clickable by passing it an `onClick` function.

<Preview>
  <Story id="components-panel--clickable"/>
</Preview>

<SectionTitle>Title</SectionTitle>

You optionally add a title to the panel. This will also add the border.

<Preview>
  <Story id="components-panel--title"/>
</Preview>

<SectionTitle>Props</SectionTitle>
<Props of={Panel} />
