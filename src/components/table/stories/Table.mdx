import { Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { Title, SectionTitle } from '../../../../.storybook/components';
import { Table } from '../Table';
import { Column } from './Column.placeholder';

<Title
  title="Table"
  subtitle="Use this component to store data and sort data in a tabular format."
/>

<SectionTitle>Simple</SectionTitle>

Store data in a table by providing the columns and the data to store.

<Preview>
  <Story id="components-table--simple"/>
</Preview>

<SectionTitle>Sort by Column</SectionTitle>

Sort data per column be setting the `sortable` column prop to `true` and handling the `onSort` callback. When a sortable column header is clicked, the `onSort` callback will be called with the column `key` and `state`. This allows for the data to be sorted then passed back into the table for rerendering.

<Preview>
  <Story id="components-table--sortable"/>
</Preview>

<SectionTitle>Empty State</SectionTitle>

If there is no data supplied to the table, provide a custom empty state component to provide the user more feedback.

<Preview>
  <Story id="components-table--empty"/>
</Preview>

<SectionTitle>Loading State</SectionTitle>

If data is asynchronously loading, set the `loading` prop to `true` in order to provide the user feedback.

<Preview>
  <Story id="components-table--loading"/>
</Preview>


<SectionTitle>Table Props</SectionTitle>
<Props of={ Table } />

<SectionTitle>Column Props</SectionTitle>
<Props of={ Column } />
