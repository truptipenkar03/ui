import { Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { Title, SectionTitle } from '../../../../.storybook/components';
import { Accordion } from '../Accordion';

<Title
  title="Accordion"
  subtitle="Accordions allow users to display content in an organized, space efficient way."
/>

<SectionTitle>Standard</SectionTitle>

The standard accordion will allow users to expand multiple sections. Set the `classic` prop to `false` to achieve this behaviour.

<Preview>
  <Story id="components-accordion--standard"/>
</Preview>

<SectionTitle>Classic</SectionTitle>

The standard accordion will allow users to expand a single section at a time. Set the `classic` prop to `true` to achieve this behaviour.

<Preview>
  <Story id="components-accordion--classic"/>
</Preview>

<SectionTitle>External Accordion Control</SectionTitle>

Take external control of the accordion by passing `selectedItems` that are managed outside of the `Accordion`.

<Preview>
  <Story id="components-accordion--external"/>
</Preview>

<SectionTitle>Props</SectionTitle>
<Props of={Accordion} />

<SectionTitle>Theme</SectionTitle>

To modify the accordion item theme, please see the `Collapse` component.
