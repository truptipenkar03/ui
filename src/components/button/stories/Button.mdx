import { Story, Preview, Props, Source, Anchor } from '@storybook/addon-docs/blocks';
import { Title, SectionTitle } from '../../../../.storybook/components';
import { Button } from '../Button';

<Title
  title="Button"
  subtitle="Buttons allow users to take actions, and make choices, with a single tap or click."
/>

<SectionTitle>Types</SectionTitle>

You can modify the `type` to change the style of the button depending on the use case.

<Preview>
  <Story id="components-button--types"/>
</Preview>

<SectionTitle>Ghost</SectionTitle>

You can modify the background of the button to white by setting the `ghost` prop.

<Preview>
  <Story id="components-button--ghost"/>
</Preview>

<SectionTitle>Disable</SectionTitle>

You can `disable` a button to limit a users ability to interact with it.

<Preview>
  <Story id="components-button--disabled"/>
</Preview>

<SectionTitle>Loading</SectionTitle>

You can add a `loading` state to a button to provide the user feedback when an asynchronous action is taking place.

<Preview>
  <Story id="components-button--loading"/>
</Preview>

<SectionTitle>Shape</SectionTitle>

You can modify the `shape` of the button depending on the use case.

<Preview>
  <Story id="components-button--shape"/>
</Preview>

<SectionTitle>Size</SectionTitle>

You can modify the `size` of the button.

<Preview>
  <Story id="components-button--size"/>
</Preview>

<SectionTitle>Props</SectionTitle>
<Props of={Button} />

